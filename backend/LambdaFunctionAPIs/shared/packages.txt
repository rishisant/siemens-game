Add lambda_role to lambda function

npm install archiver aws-sdk pg @aws-sdk/client-secrets-manager

Change build.js function name

add 
"scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "node build.js"
  }
"type": "module"

and change to "main": "index.js",
to package.json

Create API